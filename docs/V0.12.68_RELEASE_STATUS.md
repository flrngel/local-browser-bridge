# v0.12.68 release status

- Status date: 2026-09-01 UTC
- Outcome: **withdrawn after a terminal Windows packaged-acceptance failure**
- Retry status: **forbidden for this version and candidate**
- Publication status: **not published**

This document is an operational status record. It is not passing release
evidence and does not authorize another v0.12.68 candidate execution.

## Immutable candidate

| Field | Value |
| --- | --- |
| Source commit | `6f0740966ebcf1efdbfb8b795371798900cae2e9` |
| Source tree | `6b03618830229ecfa014a27b5ce308e03eb0f2c8` |
| Candidate workflow run | [`33462607975`](https://github.com/flrngel/local-browser-bridge/actions/runs/33462607975) |
| Workflow attempt | `1` |
| Release-candidate artifact ID | `9783974071` |
| Raw artifact ZIP SHA-256 | `c28a5575f527d12e30edca77ee940d59d5024856da34e60dd809bf4c8203d68c` |
| `SHA256SUMS.txt` SHA-256 | `cfb0b80a11acb56c3100d92348bb004c579b0b01b78c04c150b004500ba68093` |
| Windows server SHA-256 | `e13a0e7c37c9481fed5cfabda289afe200212df89d6fe8eface83577f571fd5c` |
| Windows helper SHA-256 | `d99d9e5d839898b981595cd3789d72a4d18b5a11ae2280ab3f31223e3eb940a4` |
| macOS universal archive SHA-256 | `1a7249d8d7319bfa8f7f50e0ca87f6a4f76b56f93ca7ac72903c3a930891921e` |
| Chromium extension ZIP SHA-256 | `9b4338ab50ce318d4d7dbc4b37f978e2687e069514d2cdc670182f9a005256a1` |

## Gates completed before Windows execution

- The source commit was the exact `main` merge commit and its tree matched the
  reviewed branch head.
- The candidate workflow completed successfully and produced the canonical
  five-file release-candidate artifact.
- Windows independently downloaded the artifact and passed the exact source,
  inventory, digest, PE identity, and five-attestation trust gate.
- Ten non-product Windows self-tests passed, including the candidate verifier,
  acceptance coordinator, automatic foreground watcher, packaged runner,
  stock-Chrome coordinator, evidence writers, helper-chain recorder, and
  screenshot sanitizer.
- The macOS quiet and deliberate-concurrency packaged lanes completed and their
  exact retained 20-file inventory was copied to Windows through the pinned
  host key, byte-rehashed, and kept outside the repository after the Windows
  failure. These files were not promoted to release evidence.

The primary macOS result hashes were:

| Result | SHA-256 |
| --- | --- |
| Aggregate | `9e2f91c71fa6e3cbf004d0b0ca71666271c0fc2b8d9bf2fb448956d5dcce0d33` |
| Quiet lane | `16bc56010ffe99f71a90fe43640461358aac609a422b760aacc62b7c2a88b0bd` |
| Deliberate-concurrency lane | `7de414f991d23ac1b6655de8c727ab1be020062b90dbbf80394bab3e9d7387d0` |

## Terminal Windows result

The Windows coordinator invoked the v0.12.68 packaged acceptance exactly once.
It created the persistent schema-2 reservation and therefore consumed the only
permitted attempt for this version.

The automatic schema-v3 foreground gate passed without a manual relay or UI
action. It accepted exactly three fresh advancing samples and proved the native
seqlock, stable external foreground and focus root, fixture-process exclusion,
interactive-session match, unchanged cursor and input desktop, and zero global
or synthetic input. Protocol-bound helper readiness, post-baseline continuity,
the baseline exact-window observation, and the baseline screenshot also passed.

The run then failed closed in the recovery suite:

```text
Timed out waiting for the signaled launch-time-only share-pump stall event.
```

The terminal coordinator classification was:

| Field | Value |
| --- | --- |
| Status | `failed-closed` |
| Stage | `runner-finished` |
| Attempt state | `runner-started-terminal` |
| Reason code | `runner-acceptance-test-failed` |
| Reservation status | `reserved-no-retry` |
| Retry allowed | `false` |
| Sanitized failure-summary SHA-256 | `5b199f2cb20847da6811569376d3de443ab0c862f6698a0cab80fd09938f6c0c` |

## Cleanup result

- No candidate server, helper, or fixture process remained.
- No candidate loopback listener remained.
- The ephemeral fixture executable was removed.
- The recovery event was released.
- Token persistence verification passed and no token was persisted.
- No unrelated process was terminated.
- The sanitized summary reported no cleanup issue.

The persistent reservation and private diagnostics remain intentionally intact.
They must not be deleted, rewritten, or used to justify a retry.

## Downstream status

The Windows packaged acceptance did not pass, so all dependent release steps
were stopped:

- stock-Chrome packaged acceptance was not started;
- no canonical v0.12.68 evidence branch or evidence commit was created;
- no schema-3 acceptance receipt was created;
- `publish.yml` was not dispatched; and
- no `v0.12.68` tag or GitHub Release was created.

## Required successor work

v0.12.68 cannot be retried. A successor must diagnose and repair the
launch-time recovery fault-event signaling boundary, align the Rust package and
Chromium extension on a new version, build a new immutable candidate from a new
`main` source commit, and collect entirely fresh platform evidence.
