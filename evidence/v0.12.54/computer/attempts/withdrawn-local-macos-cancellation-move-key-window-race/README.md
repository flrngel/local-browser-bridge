# Withdrawn v0.12.54 local macOS move-receiver diagnostic

This is terminal local negative evidence, not release evidence.

The source-built v0.12.54 package came from clean detached commit
`fc696dc` after every test and the full local deployment build passed. Its
local checksum-manifest SHA-256 was
`e47129c3b73b73a7fc565251a6d6780288862b4374381362d71545f294ef56a0`,
and its macOS universal archive SHA-256 was
`d70ec0ec3a87786b6fe75cf343f0fe9f2faaf2355777893395cf78d65bb6cdd2`.
The diagnostic directly invoked the release harness with explicitly synthetic
binding identifiers. No GitHub release-candidate workflow or artifact existed,
and the harness status vocabulary must not be interpreted as candidate proof.

The local quiet diagnostic passed 158 checks, then the cancellation-bound
`computer.move` did not advance the primary fixture's mouse-move
instrumentation within ten seconds. Its retained diagnosis records no
acknowledged product action, `moveEvents: 61 -> 61`, an unchanged functional
fixture state, a restored same-process sibling receiver, and unchanged
foreground, user focus, hardware cursor, Space, and sampled HID boundaries.
It demonstrated that repeatedly observing AX main/focused state did not prove
that AppKit had committed the requested window as its actual event receiver.

The attempt was not retried. A second planned local repetition never started.
No GitHub candidate, deliberate macOS lane, Windows, Chrome, evidence
aggregate, publication, tag, or Release existed. Candidate and fixture
processes stopped and scratch storage was removed. Only the allowlisted
sanitized result, log, and six fixture-only screenshots are retained here.

Version 0.12.55 posts the already-audited target-only make-key pair after the
bounded no-raise focus transition, re-proves the requested-active phase, and
only then permits trajectory dispatch. The prior exact receiver is still
restored before returning.
