# Withdrawn v0.12.39 macOS attempt 2

This immutable negative record belongs to workflow run `33051081816`, attempt
`2`, release-candidate artifact `9638514531`, and source
`f402c019edc1892ac2f040c5d6e4c60a8fe46e4a`.

The fresh quiet lane passed and its six reviewed screenshots contained only the
test fixture. The subsequent deliberate-concurrency lane stopped before any
candidate process or app-share action. Native quiet-seat monitoring observed
shared-seat keyboard, pointer, and focus changes, then failed closed when the
foreground AX identity became unreadable. The result records
`completedBeforeCandidateExecution: false`, `actionDispatched: false`, and no
operator markers or deliberate screenshots were produced.

The exact run/attempt/artifact was withdrawn. Windows acceptance, stock-Chrome
acceptance, tagging, and release publication were not started from this
attempt. These bytes must not be resumed, merged into successful evidence, or
relabelled for another workflow attempt.

The retained files are the complete quiet-lane result, log, and six screenshots,
plus the deliberate pre-execution failure result and log. A sanitized leakage
scan found no bearer or GitHub token, user/home or temporary path, email,
loopback endpoint, or OAuth value.
